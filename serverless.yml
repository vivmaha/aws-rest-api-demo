service: periodic-table

provider:
  name: aws
  runtime: nodejs10.x
  stage: dev
  region: us-west-2

package:
  exclude:
    - ./**
  include:
    - ./build/**

functions:
  getElements:
    handler: build/functions/elements.getElements
    events:
      - http: 
          path: elements
          method: get